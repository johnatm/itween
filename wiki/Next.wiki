#summary Where I am and and what needs to be done next development session
 * Rename/rethnink curveTo and moveToBezier interface naming!
 * Finish curve! Lookto, froms, loops
 * //CHECK ON var parsedBeziers: Array = ParseBeziers(beziers,args["looped"]);
 * MoveToBezier
 * CameraFade (color)
 * New beziers!
 * New Wordpress powered support site
 * New examples
 * Ensure old videos are stated as old
 * setTimout()?
 * setInterval()?
 * look at all compiled emails on bezier/splines/curves